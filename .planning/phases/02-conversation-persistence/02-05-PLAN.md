---
phase: 02-conversation-persistence
plan: "05"
type: execute
wave: 4
depends_on:
  - 02-04
files_modified: []
autonomous: false
requirements:
  - CHAT-03
  - UI-02

must_haves:
  truths:
    - "User can send a message and see it persist after browser reload"
    - "Sidebar lists all past conversations with title and relative date"
    - "Clicking a sidebar entry loads that conversation's prior messages into the chat area"
    - "New chat button creates a fresh session"
    - "Delete removes a conversation from the sidebar"
  artifacts: []
  key_links: []
---

<objective>
Human verification that the full end-to-end conversation persistence flow works in a browser — streaming, localStorage session persistence, sidebar navigation with message history loading, and delete.

Purpose: Automated tests cover backend logic and TypeScript compilation, but the browser interaction (localStorage persistence across tab close/reopen, SSE streaming display, prior message history rendering, sidebar rendering with relative dates) requires human eyes.
Output: Confirmation that Phase 2 success criteria are met, or a list of issues to fix.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-conversation-persistence/02-CONTEXT.md
</context>

<tasks>

<!-- Task 1 is type="auto": Claude runs scripted startup commands.
     The plan is autonomous: false because Task 2 is a blocking human checkpoint.
     Task 1 completes autonomously before Task 2 pauses for the user. -->
<task type="auto">
  <name>Task 1: Start backend and frontend servers</name>
  <files></files>
  <action>
    Start the Go backend (in a terminal, leave running):
    ```bash
    cd /Users/baotoq/Work/open-jarvis/src && go run ./cmd/main.go -f etc/config.yaml
    ```
    The backend should start on port 8888. A `data/` directory will be created for the SQLite file.

    Start the Next.js frontend (in another terminal, leave running):
    ```bash
    cd /Users/baotoq/Work/open-jarvis/frontend && npm run dev
    ```
    Frontend starts on http://localhost:3000.
  </action>
  <verify>
    ```bash
    curl -s http://localhost:8888/api/conversations && echo "backend ok"
    ```
    Returns `[]` (empty array) without error.
  </verify>
  <done>Backend serves GET /api/conversations (returns `[]` for empty). Frontend dev server is running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end persistence and sidebar in browser</name>
  <files></files>
  <action>
    Open http://localhost:3000 and run through 6 browser tests to confirm Phase 2 success criteria.

    **Test 1 — First message and persistence:**
    1. Type a message (e.g. "Hello, what is 2+2?") and send
    2. Response streams token-by-token in the chat area
    3. Conversation appears in the sidebar with the first message as title + "just now"
    4. DevTools → Application → Local Storage → `jarvis-session-id` key exists with a UUID

    **Test 2 — Browser reload persistence:**
    1. Reload the page (Cmd+R)
    2. Same conversation is still in the sidebar
    3. `jarvis-session-id` in localStorage has the same UUID

    **Test 3 — Message history loads on sidebar click:**
    1. Send a second message in the same conversation to build some history
    2. Click "New chat" to start a fresh session
    3. Click the original conversation in the sidebar
    4. Prior messages from that conversation appear in the chat area

    **Test 4 — New conversation:**
    1. Click "New chat" in the sidebar
    2. Send a different message
    3. Both conversations appear in sidebar, newer one at the top

    **Test 5 — Switch conversations:**
    1. Click the first conversation in the sidebar
    2. Clicked entry appears highlighted/active and its messages load in the chat area

    **Test 6 — Delete conversation:**
    1. Hover over a conversation entry
    2. Click the Delete button
    3. Conversation disappears from sidebar without page reload
  </action>
  <verify>
    All 6 tests pass. Type "approved" to continue, or describe any issues found.
  </verify>
  <done>All 6 manual browser tests pass. Phase 2 success criteria confirmed.</done>
</task>

</tasks>

<verification>
All 6 manual test flows pass in the browser.
</verification>

<success_criteria>
- Streaming chat works end-to-end (tokens appear in real time)
- Conversation appears in sidebar after first message
- Session ID persists across browser reload
- Clicking a sidebar entry loads the conversation's prior messages into the chat area
- Multiple conversations visible in sidebar, newest first
- Delete removes conversation from sidebar
- Backend SQLite file exists at `src/data/conversations.db` after sending messages
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversation-persistence/02-05-SUMMARY.md`
</output>
