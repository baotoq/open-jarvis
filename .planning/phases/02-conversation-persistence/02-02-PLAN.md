---
phase: 02-conversation-persistence
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/next.config.ts
  - frontend/tsconfig.json
  - frontend/app/globals.css
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/lib/utils.ts
  - frontend/components/ui/button.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Next.js app scaffolds and `npm run dev` starts without errors"
    - "Tailwind v4 CSS-first config is in place (no tailwind.config.ts)"
    - "shadcn/ui button component is initialized and importable"
    - "`cn()` utility is available in lib/utils.ts"
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "Tailwind v4 @import directive, base CSS"
      contains: "@import \"tailwindcss\""
    - path: "frontend/app/layout.tsx"
      provides: "Root layout wrapping the app"
    - path: "frontend/lib/utils.ts"
      provides: "cn() helper"
      contains: "clsx"
    - path: "frontend/next.config.ts"
      provides: "Next.js config"
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "frontend/app/globals.css"
      via: "import './globals.css'"
      pattern: "globals.css"
---

<objective>
Scaffold the Next.js 15 frontend application from scratch in the `frontend/` directory. Install Tailwind v4, initialize shadcn/ui, and create the root layout + landing page shell.

Purpose: Phase 1 frontend plans (01-02, 01-03) have not yet executed. This plan creates the frontend foundation needed for the Phase 2 UI work (sidebar + chat area) in Plan 04. It can run in parallel with Plan 01 since there are no shared files.
Output: A running Next.js app at `frontend/` with Tailwind v4, shadcn/ui initialized, cn() utility, and root layout.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-conversation-persistence/02-CONTEXT.md
@.planning/phases/02-conversation-persistence/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 app with Tailwind v4</name>
  <files>
    frontend/package.json,
    frontend/tsconfig.json,
    frontend/next.config.ts,
    frontend/app/globals.css,
    frontend/app/layout.tsx,
    frontend/app/page.tsx
  </files>
  <action>
    Run from the repo root (NOT from src/):

    ```bash
    npx create-next-app@latest frontend \
      --typescript \
      --tailwind \
      --eslint \
      --app \
      --no-src-dir \
      --import-alias "@/*"
    ```

    After scaffolding, verify `frontend/app/globals.css` uses the Tailwind v4 CSS-first pattern. The create-next-app output may produce Tailwind v3 directives (`@tailwind base` etc.) — if so, replace globals.css content with:
    ```css
    @import "tailwindcss";
    ```
    Do NOT create `tailwind.config.ts` — Tailwind v4 uses CSS-first config only.

    Update `frontend/app/layout.tsx` to:
    - Import globals.css
    - Set metadata: `title: "Jarvis"`, `description: "Personal AI Assistant"`
    - Render `<html lang="en"><body>{children}</body></html>`

    Update `frontend/app/page.tsx` to a minimal server component placeholder:
    ```tsx
    export default function Home() {
      return <main className="min-h-screen">Loading...</main>
    }
    ```

    Add `NEXT_PUBLIC_API_URL` support in frontend code. In `frontend/lib/api.ts` (create this file):
    ```typescript
    export const API_BASE = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:8888'
    ```
    This establishes the env var convention for all backend calls.

    Add `frontend/.env.local` (not committed, just for local dev):
    ```
    NEXT_PUBLIC_API_URL=http://localhost:8888
    ```
    Add `frontend/.env.local` to `.gitignore` if not already excluded by `*.local` pattern.
  </action>
  <verify>
    ```bash
    cd /Users/baotoq/Work/open-jarvis/frontend && npm run build 2>&1 | tail -5
    ```
    Build should succeed with no errors.
  </verify>
  <done>
    `npm run build` exits 0. `frontend/app/globals.css` contains `@import "tailwindcss"`. No `tailwind.config.ts` file exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and cn() utility</name>
  <files>
    frontend/lib/utils.ts,
    frontend/components/ui/button.tsx,
    frontend/package.json
  </files>
  <action>
    From the `frontend/` directory, initialize shadcn/ui:
    ```bash
    cd /Users/baotoq/Work/open-jarvis/frontend
    npx shadcn@latest init --defaults
    npx shadcn@latest add button
    ```

    The init command creates `frontend/lib/utils.ts` with the `cn()` helper (clsx + tailwind-merge). Verify this file exists after init.

    If shadcn init creates a `tailwind.config.ts` file (it may for compatibility reasons), delete it. The project uses Tailwind v4 CSS-first config exclusively. If shadcn writes `@tailwind` directives into globals.css, restore the `@import "tailwindcss"` directive.

    Verify that `frontend/lib/utils.ts` exports `cn`:
    ```typescript
    import { clsx, type ClassValue } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    Install date-fns for use in Plan 04:
    ```bash
    cd /Users/baotoq/Work/open-jarvis/frontend && npm install date-fns
    ```
  </action>
  <verify>
    ```bash
    cd /Users/baotoq/Work/open-jarvis/frontend && npm run build 2>&1 | tail -5
    ```
    Build succeeds. `frontend/lib/utils.ts` exports `cn`. `frontend/components/ui/button.tsx` exists.
  </verify>
  <done>
    `npm run build` exits 0. `frontend/lib/utils.ts` contains `cn` export. `frontend/components/ui/button.tsx` exists. `date-fns` in package.json dependencies. No `tailwind.config.ts` file exists.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/baotoq/Work/open-jarvis/frontend && npm run build
ls /Users/baotoq/Work/open-jarvis/frontend/lib/utils.ts
ls /Users/baotoq/Work/open-jarvis/frontend/components/ui/button.tsx
grep -l "tailwind.config" /Users/baotoq/Work/open-jarvis/frontend/ 2>/dev/null || echo "no tailwind.config (correct)"
```
</verification>

<success_criteria>
- `npm run build` in frontend/ exits 0 with no TypeScript or build errors
- `frontend/app/globals.css` uses `@import "tailwindcss"` (Tailwind v4)
- No `tailwind.config.ts` file exists
- `frontend/lib/utils.ts` exports `cn()`
- `frontend/components/ui/button.tsx` exists (shadcn/ui initialized)
- `date-fns` is in `frontend/package.json` dependencies
- `frontend/lib/api.ts` exports `API_BASE`
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversation-persistence/02-02-SUMMARY.md`
</output>
