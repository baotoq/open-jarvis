---
phase: 03-file-and-shell-tools
plan: 05
type: execute
wave: 3
depends_on:
  - 03-03
  - 03-04
files_modified: []
autonomous: false
requirements:
  - TOOL-01
  - TOOL-02
  - SAFE-01
  - SAFE-02
  - UI-01

must_haves:
  truths:
    - "User can ask agent to read a file and see its contents in the conversation"
    - "User can ask agent to create or modify a file and verify the change on disk"
    - "User can ask agent to run a shell command and see the output inline in the chat"
    - "Agent prompts user for confirmation before executing shell commands flagged by denylist"
    - "Tool calls and their results appear as distinct inspectable blocks in the chat UI"
  artifacts:
    - path: "src/backend/internal/toolexec/executor.go"
      provides: "Executor interface"
    - path: "src/backend/internal/toolexec/filetool.go"
      provides: "FileTool"
    - path: "src/backend/internal/toolexec/shelltool.go"
      provides: "ShellTool"
    - path: "src/backend/internal/logic/chatlogic.go"
      provides: "Agentic loop"
    - path: "src/backend/internal/handler/approvehandler.go"
      provides: "Approval endpoint"
    - path: "src/frontend/components/ToolCallBlock.tsx"
      provides: "Tool call display"
    - path: "src/frontend/components/ApprovalDialog.tsx"
      provides: "Approval dialog"
  key_links:
    - from: "browser chat input"
      to: "agent tool execution"
      via: "SSE stream → tool_call event → backend tool dispatch"
    - from: "approval dialog"
      to: "backend shell execution"
      via: "submitApproval POST → ApprovalStore.Resolve → shell_run proceeds"
---

<objective>
Human browser verification of all Phase 3 capabilities end-to-end.

Purpose: Automated tests cover the logic but cannot verify visual rendering, dialog interactions, or SSE event rendering in the actual browser. This checkpoint confirms the full tool loop works as an integrated system.

Output: Human confirmation that all 5 phase success criteria are met.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-file-and-shell-tools/03-03-SUMMARY.md
@.planning/phases/03-file-and-shell-tools/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start services and run full backend test suite</name>
  <files></files>
  <action>
    Run the full test suite to confirm nothing is broken before human verification:
    ```bash
    cd /Users/baotoq/Work/open-jarvis/src/backend && go test ./... -count=1 -cover
    cd /Users/baotoq/Work/open-jarvis/src/frontend && npm run build
    ```

    If all tests pass and build succeeds, start both services:
    ```bash
    # Terminal 1 — backend (from src/backend/)
    go run ./cmd/main.go

    # Terminal 2 — frontend (from src/frontend/)
    npm run dev
    ```

    Confirm backend is responding:
    ```bash
    curl -s http://localhost:8888/api/conversations | head -c 100
    ```

    Report test results and any failures before the human verification step.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/open-jarvis/src/backend && go test ./... -count=1 && cd /Users/baotoq/Work/open-jarvis/src/frontend && npm run build</automated>
  </verify>
  <done>All backend tests pass; frontend build succeeds; both services start without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of all 5 Phase 3 success criteria</name>
  <action>Human verifies tool calls, file operations, shell approval, and UI display in the browser.</action>
  <what-built>
    Phase 3 complete tool integration:
    - File tool (read_file, write_file) with path safety
    - Shell tool (shell_run) with allowlist/denylist from config
    - Agentic loop in ChatLogic — model can chain tool calls
    - Approval gate — shell commands blocked pending user confirmation
    - ToolCallBlock — collapsible tool call + result display in chat
    - ApprovalDialog — approve/deny modal for shell commands
    - POST /api/chat/approve endpoint
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in a browser. Verify all 5 scenarios:

    **Scenario 1 — File read (TOOL-01)**
    1. Create a test file first: `echo "hello from jarvis" > /tmp/test.txt`
    2. Ask the agent: "Please read the file at /tmp/test.txt and tell me what it says"
    3. Expected: ToolCallBlock appears showing read_file call and the file contents
    4. Expected: Agent responds with the file contents in text

    **Scenario 2 — File write (TOOL-01)**
    1. Ask: "Create a file at /tmp/jarvis-test-output.txt with the content 'Phase 3 works'"
    2. Expected: ToolCallBlock appears showing write_file call with the path and content
    3. Verify on disk: `cat /tmp/jarvis-test-output.txt` should print "Phase 3 works"

    **Scenario 3 — Shell command auto-approved (TOOL-02, SAFE-01)**
    If config.yaml has `ls *` in ShellAllowlist:
    1. Ask: "Run ls /tmp and show me the output"
    2. Expected: NO dialog appears (auto-approved), ToolCallBlock shows shell_run result inline

    **Scenario 4 — Shell command approval gate (SAFE-02, SAFE-01)**
    1. Ask: "Run the command: echo hello world"
    2. Expected: Approval dialog appears showing the command "echo hello world"
    3. Click "Allow" — Expected: dialog closes, ToolCallBlock shows result with "hello world"
    4. Ask another shell command, click "Deny" — Expected: agent reports command was denied

    **Scenario 5 — Tool blocks visible in chat (UI-01)**
    1. After the above interactions, scroll through the conversation
    2. Expected: Tool call blocks are visually distinct from text messages
    3. Click a ToolCallBlock to expand it — Expected: shows args JSON and result
    4. Reload the page — Expected: conversation reloads; historical text messages display correctly (tool blocks from prior session may not re-render as blocks, which is acceptable — the backend does not persist MessagePart structure)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 scenarios work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 5 phase success criteria confirmed by human browser testing.
</verification>

<success_criteria>
- User confirmed: file read shows contents in ToolCallBlock
- User confirmed: file write creates file on disk
- User confirmed: shell commands with denylist trigger approval dialog
- User confirmed: approve/deny buttons work correctly
- User confirmed: ToolCallBlock is visible and expandable in chat UI
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-and-shell-tools/03-05-SUMMARY.md`
</output>
