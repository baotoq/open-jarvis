---
phase: 07-group-be-to-related-domain-instead-of-flat
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/backend/cmd/main.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "cmd/main.go imports from the three new domain handler packages"
    - "Old internal/handler/ and internal/logic/ directories no longer exist"
    - "go build ./... passes with no errors"
    - "go test ./... passes with no failures"
  artifacts:
    - path: "src/backend/cmd/main.go"
      provides: "Route registration using new domain handler packages"
      contains: "open-jarvis/internal/chat/handler"
    - path: "src/backend/cmd/main.go"
      provides: "conv domain import present"
      contains: "open-jarvis/internal/conv/handler"
    - path: "src/backend/cmd/main.go"
      provides: "cfg domain import present (package cfg, not package config)"
      contains: "open-jarvis/internal/config/handler"
  key_links:
    - from: "src/backend/cmd/main.go"
      to: "open-jarvis/internal/chat/handler"
      via: "import + handler function calls"
      pattern: "chat\\.ChatStreamHandler"
    - from: "src/backend/cmd/main.go"
      to: "open-jarvis/internal/conv/handler"
      via: "import + handler function calls"
      pattern: "conv\\.ListConversationsHandler"
    - from: "src/backend/cmd/main.go"
      to: "open-jarvis/internal/config/handler"
      via: "import + handler function calls"
      pattern: "cfg\\.GetConfigHandler"
---

<objective>
Wire everything together: update `cmd/main.go` to import the three new domain handler packages, delete the now-empty flat `internal/handler/` and `internal/logic/` directories, and verify the full build and test suite passes.

Purpose: Completes the refactor. After this plan the codebase only contains domain-grouped packages with no remnants of the flat layout.
Output: Updated `cmd/main.go`, deleted old directories, clean `go build ./...` and `go test ./...`.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-group-be-to-related-domain-instead-of-flat/07-02-SUMMARY.md
</context>

<interfaces>
<!-- Current cmd/main.go imports (to be replaced): -->
<!--   "open-jarvis/internal/handler"                                      -->
<!-- New imports needed:                                                    -->
<!--   "open-jarvis/internal/chat/handler"    → qualifier: chat            -->
<!--   "open-jarvis/internal/conv/handler"    → qualifier: conv            -->
<!--   "open-jarvis/internal/config/handler"  → qualifier: cfg             -->
<!--                                                                        -->
<!-- IMPORTANT: internal/config/handler declares `package cfg` (NOT        -->
<!--   `package config`). The qualifier in call sites is `cfg.`, not       -->
<!--   `config.`. No import alias is needed — cfg is the declared name.    -->

<!-- Handler function mapping (old → new call syntax):                     -->
<!--   handler.ChatStreamHandler(svcCtx)           → chat.ChatStreamHandler(svcCtx)           -->
<!--   handler.ApproveHandler(svcCtx)              → chat.ApproveHandler(svcCtx)              -->
<!--   handler.ListConversationsHandler(svcCtx)    → conv.ListConversationsHandler(svcCtx)    -->
<!--   handler.GetConversationHandler(svcCtx)      → conv.GetConversationHandler(svcCtx)      -->
<!--   handler.GetConversationMessagesHandler(svcCtx) → conv.GetConversationMessagesHandler(svcCtx) -->
<!--   handler.DeleteConversationHandler(svcCtx)   → conv.DeleteConversationHandler(svcCtx)   -->
<!--   handler.GetConfigHandler(svcCtx)            → cfg.GetConfigHandler(svcCtx)             -->
<!--   handler.UpdateConfigHandler(svcCtx)         → cfg.UpdateConfigHandler(svcCtx)          -->
<!--   handler.SearchConversationsHandler(svcCtx)  → conv.SearchConversationsHandler(svcCtx)  -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update cmd/main.go and delete old directories</name>
  <files>src/backend/cmd/main.go</files>
  <action>
    1. Read `src/backend/cmd/main.go` (current content uses `"open-jarvis/internal/handler"`).

    2. Replace the single handler import with three domain-specific imports. The new package names are:
       - `internal/chat/handler` declares `package chat` → qualifier `chat`
       - `internal/conv/handler` declares `package conv` → qualifier `conv`
       - `internal/config/handler` declares `package cfg` → qualifier `cfg`
         NOTE: the directory is `config/handler` but the Go package declaration is `package cfg`.
         Do NOT use `config.` as a qualifier — it will not compile. The qualifier must be `cfg.`.
         No import alias is needed since cfg is already the declared package name.

       New import block (replace `"open-jarvis/internal/handler"` with):
       ```go
       "open-jarvis/internal/chat/handler"
       "open-jarvis/internal/config/handler"
       "open-jarvis/internal/conv/handler"
       ```
       (gofmt will group these; import alias is not needed since package names are distinct: chat, conv, cfg)

    3. Replace all handler function call qualifiers:
       - `handler.ChatStreamHandler` → `chat.ChatStreamHandler`
       - `handler.ApproveHandler` → `chat.ApproveHandler`
       - `handler.ListConversationsHandler` → `conv.ListConversationsHandler`
       - `handler.GetConversationHandler` → `conv.GetConversationHandler`
       - `handler.GetConversationMessagesHandler` → `conv.GetConversationMessagesHandler`
       - `handler.DeleteConversationHandler` → `conv.DeleteConversationHandler`
       - `handler.SearchConversationsHandler` → `conv.SearchConversationsHandler`
       - `handler.GetConfigHandler` → `cfg.GetConfigHandler`
       - `handler.UpdateConfigHandler` → `cfg.UpdateConfigHandler`

    4. Delete the old flat directories (now superseded by domain subdirectories):
       - `rm -rf src/backend/internal/handler/`
       - `rm -rf src/backend/internal/logic/`

    5. Run `gofmt -w src/backend/cmd/main.go` to ensure correct formatting.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/open-jarvis/src/backend && go build ./... && go test ./...</automated>
  </verify>
  <done>
    - `cmd/main.go` imports `open-jarvis/internal/chat/handler`, `open-jarvis/internal/conv/handler`, `open-jarvis/internal/config/handler`
    - Call sites use `chat.`, `conv.`, `cfg.` qualifiers (not `config.`)
    - `internal/handler/` directory does not exist
    - `internal/logic/` directory does not exist
    - `go build ./...` exits 0
    - `go test ./...` exits 0 (all tests pass)
  </done>
</task>

</tasks>

<verification>
Final state checks:
1. `cd src/backend && go build ./...` — exits 0, no errors
2. `cd src/backend && go test ./...` — all tests pass
3. `ls src/backend/internal/` — shows `chat/`, `conv/`, `config/`, `svc/`, `types/`, `toolexec/` (no flat `handler/` or `logic/`)
4. `go vet ./...` — no issues
</verification>

<success_criteria>
- `go build ./...` and `go test ./...` both pass from `src/backend/`
- `internal/handler/` and `internal/logic/` no longer exist
- Domain structure: `internal/chat/`, `internal/conv/`, `internal/config/` each with `handler/` and `logic/` subdirectories
- `cmd/main.go` uses new domain package qualifiers (`chat.`, `conv.`, `cfg.`)
- No behavior changes — refactor only
</success_criteria>

<output>
After completion, create `.planning/phases/07-group-be-to-related-domain-instead-of-flat/07-03-SUMMARY.md`
</output>
