---
phase: 05-configuration-and-search
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified: []
autonomous: false
requirements:
  - CHAT-04
  - UI-03
  - MEM-01

must_haves:
  truths:
    - "User can navigate to /settings and see current model config fields"
    - "User can change a field and save — next page reload shows the updated value"
    - "Config change persists across backend restart"
    - "User can type in the sidebar search box and see matching conversations"
    - "Clicking a search result loads that conversation"
  artifacts: []
  key_links: []
---

<objective>
Human browser verification of all Phase 5 capabilities end-to-end.

Purpose: Confirms that the settings UI, config persistence, and conversation search work correctly in the real browser against the running backend. Automated tests cover units; this verifies the integrated system.
Output: User confirmation that all three requirements (CHAT-04, UI-03, MEM-01) are satisfied.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 complete: settings page (CHAT-04, UI-03) and conversation search (MEM-01).

    - GET/PUT /api/config — model provider config API
    - GET /api/conversations/search?q= — FTS5 full-text search
    - /settings page — controlled form for BaseURL, Name, APIKey, SystemPrompt
    - Sidebar search input with 300ms debounce
    - Gear icon in Sidebar header links to /settings
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    ```bash
    cd src/backend && mkdir -p data && go run ./cmd/main.go
    # In a separate terminal:
    cd src/frontend && npm run dev
    ```
    Open http://localhost:3000

    **Test 1 — Settings page navigation:**
    1. Click the gear icon (Settings2) in the sidebar header
    2. Verify browser navigates to http://localhost:3000/settings
    3. Verify four fields render: Base URL, Model Name, API Key, System Prompt
    4. Verify fields are pre-populated with values from the backend (not empty)

    **Test 2 — Config save and persistence:**
    1. On /settings, change the Model Name field to "gpt-4o-mini"
    2. Click Save
    3. Verify "Saved" green confirmation appears
    4. Reload the page (Cmd+R)
    5. Verify the Model Name field still shows "gpt-4o-mini"
    6. Bonus: Stop and restart the backend (`go run ./cmd/main.go`), reload /settings — value persists

    **Test 3 — Back navigation:**
    1. On /settings, click the "← Back" link
    2. Verify navigation returns to http://localhost:3000

    **Test 4 — Conversation search:**
    1. Have at least 1 past conversation (send a message if not)
    2. In the sidebar search input, type a word from a past message (e.g. "hello")
    3. Verify results appear within ~1 second (300ms debounce + network)
    4. Verify result shows conversation title and a snippet with highlighted word (bold tags)
    5. Click a search result — verify the conversation loads in ChatArea
    6. Clear the search input — verify the full conversation list is restored

    **Test 5 — Empty search:**
    1. Type then clear the search input
    2. Verify full conversation list restores (no blank state)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
Human sign-off required.
</verification>

<success_criteria>
- All 5 browser tests pass
- Config changes visible after page reload
- Config changes visible after backend restart
- Search returns results matching past conversation content
- No console errors during normal usage
</success_criteria>

<output>
After completion, create `.planning/phases/05-configuration-and-search/05-05-SUMMARY.md`
</output>
