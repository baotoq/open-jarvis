---
phase: 06-add-go-linting
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified: []
autonomous: false
requirements:
  - lint-clean-exit
must_haves:
  truths:
    - "golangci-lint run ./... exits 0 with zero issues reported"
    - "go test -cover ./... passes with all tests green"
    - "The binary is installed, the config is in place, and the codebase is clean"
  artifacts:
    - path: "src/backend/.golangci.yml"
      provides: "validated, working golangci-lint configuration"
      contains: "version: \"2\""
  key_links:
    - from: "src/backend/.golangci.yml"
      to: "src/backend internal packages"
      via: "golangci-lint run ./..."
      pattern: "exit 0"
---

<objective>
Final gate: confirm golangci-lint exits 0 and all tests pass, then get human verification sign-off.

Purpose: Wave 2 plans (02 and 03) run in parallel and touch different files. This plan merges the results with a clean-exit check and human confirmation. No code changes are expected — if any issues remain, they must be fixed here before sign-off.

Output: Phase 6 complete. golangci-lint runs cleanly on every commit from this point forward.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-add-go-linting/06-02-SUMMARY.md
@.planning/phases/06-add-go-linting/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full lint and test suite — fix any remaining issues</name>
  <files>any files with remaining lint issues (determined at runtime)</files>
  <action>
Run the full lint suite and test suite from src/backend/:

```bash
cd src/backend && golangci-lint run ./...
cd src/backend && go test -cover ./...
```

If golangci-lint run ./... exits non-zero:
1. Read the reported issues carefully
2. Fix each remaining issue using the same patterns from Plan 02 (errcheck) and Plan 03 (revive/unused/ineffassign)
3. Re-run until exit 0

If go test -cover ./... reports failures:
1. Read the test output
2. Fix the regression without weakening assertions
3. Re-run until all tests pass

Only proceed to Task 2 when BOTH commands exit 0.

Document what was found and fixed (if anything) — this will go into the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/open-jarvis/src/backend && golangci-lint run ./... && go test -cover ./... 2>&1 | tail -30</automated>
  </verify>
  <done>golangci-lint run ./... exits 0 with no issues. go test -cover ./... exits 0 with all tests passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — confirm clean lint and test output</name>
  <action>Present the human with the verification steps below and wait for their confirmation.</action>
  <what-built>
golangci-lint v2.11.3 is installed. src/backend/.golangci.yml is configured with errcheck, ineffassign, staticcheck, revive, and unused enabled. All 50 pre-existing lint issues have been fixed. The codebase passes golangci-lint run ./... with zero issues and go test -cover ./... with all tests green.
  </what-built>
  <how-to-verify>
Run these two commands from src/backend/ and confirm both exit cleanly:

1. `cd src/backend && golangci-lint run ./...`
   - Expected: no output, exit code 0
   - If you see issues: type them in your response

2. `cd src/backend && go test -cover ./...`
   - Expected: all packages listed as ok, exit code 0
   - If you see failures: type them in your response

3. Confirm the command is documented: `grep "golangci-lint" /Users/baotoq/Work/open-jarvis/CLAUDE.md`
   - Expected: at least one line showing golangci-lint run ./...
  </how-to-verify>
  <verify>
    <automated>MISSING — human visual verification required</automated>
  </verify>
  <done>Human types "approved" confirming lint exits 0, tests pass, and CLAUDE.md is updated.</done>
  <resume-signal>Type "approved" if all three checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 6 is complete when:
1. `cd src/backend && golangci-lint run ./...` exits 0 with zero issues
2. `cd src/backend && go test -cover ./...` exits 0 with all tests passing
3. Human has confirmed both commands and the CLAUDE.md documentation
</verification>

<success_criteria>
- golangci-lint run ./... exits 0 — zero lint issues across the entire Go backend
- go test -cover ./... exits 0 — no test regressions introduced by lint fixes
- CLAUDE.md documents golangci-lint run ./... in the Go commands section
- Human sign-off received
</success_criteria>

<output>
After completion, create .planning/phases/06-add-go-linting/06-04-SUMMARY.md
</output>
