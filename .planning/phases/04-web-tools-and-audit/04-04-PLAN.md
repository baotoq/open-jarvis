---
phase: 04-web-tools-and-audit
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - TOOL-03
  - TOOL-04
  - SAFE-04

must_haves:
  truths:
    - "User can ask agent to search the web and receive summarized results from multiple sources"
    - "User can ask agent to fetch and summarize a specific web page"
    - "Every tool execution is recorded in audit log (verifiable via sqlite3 CLI)"
  artifacts:
    - path: "src/backend/data/conversations.db"
      provides: "SQLite DB with tool_audit_log table populated after tool use"
      contains: "tool_audit_log"
  key_links:
    - from: "browser chat UI"
      to: "src/backend/data/conversations.db tool_audit_log table"
      via: "agentic loop -> AuditStore.Log after each tool execution"
      pattern: "tool_audit_log"
---

<objective>
Human verification that all Phase 4 capabilities work end-to-end: web search, web fetch, and audit logging all function correctly in the running system.

Purpose: Confirm that web tools return real results and that the audit trail records every execution.
Output: Phase 4 verified complete.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/backend/etc/config.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: web fetch, web search, and audit log work end-to-end</name>
  <action>Human verifies the complete Phase 4 capability by running the backend and frontend and exercising all three features.</action>
  <what-built>
    - WebFetchTool: agent can fetch and extract readable text from any URL
    - WebSearchTool: agent can search the web via Brave Search API (requires BraveSearchAPIKey in config)
    - AuditStore: every tool call logged to tool_audit_log in conversations.db
    - All tools registered in ToolRegistry and defined in chatTools
  </what-built>
  <how-to-verify>
    **Pre-requisite:** Start backend with `cd src/backend && go run ./cmd/main.go` and frontend with `cd src/frontend && npm run dev`

    **Test 1 — Web Fetch (TOOL-03):**
    1. Open http://localhost:3000 in browser
    2. Type: "Please fetch https://go.dev and summarize what you find"
    3. Expected: Agent returns a summary containing text from the Go homepage (title "The Go Programming Language" or similar, with readable paragraph text — NOT raw HTML tags)
    4. Verify: No raw `<div>`, `<script>`, or `<html>` tags appear in the response

    **Test 2 — Web Search (TOOL-04):**
    (Requires BraveSearchAPIKey set in src/backend/etc/config.yaml)
    1. Type: "Search the web for 'Go programming language tutorial' and tell me the top results"
    2. Expected: Agent returns a numbered list of 3-5 results with titles, URLs, and descriptions
    3. If BraveSearchAPIKey is not configured: agent should respond with "web_search not configured" error message — this is acceptable

    **Test 3 — Audit Log (SAFE-04):**
    After completing Test 1 and/or Test 2, inspect the audit log:
    ```bash
    cd src/backend
    sqlite3 data/conversations.db "SELECT tool_name, args_json, substr(result,1,100), error FROM tool_audit_log ORDER BY timestamp DESC LIMIT 10;"
    ```
    Expected output: rows with tool_name="web_fetch" or "web_search", args_json with the URL/query, result with first 100 chars of extracted text

    **Test 4 — Regression (existing tools still work):**
    1. Type: "Run the command: echo hello world"
    2. Expected: approval dialog appears (shell_run approval gate still works)
    3. Approve it; expected output: "hello world"
    4. Check audit log again — should now also have a shell_run entry
  </how-to-verify>
  <verify>Human inspection — see how-to-verify steps above</verify>
  <done>All four tests pass: web_fetch returns readable text, web_search returns results or clear error, audit log has rows, shell_run approval gate works</done>
  <resume-signal>Type "approved" if all tests pass, or describe which test failed and the error seen</resume-signal>
</task>

</tasks>

<verification>
All four manual verification tests pass. Audit log contains entries for web_fetch, web_search (if configured), and shell_run tool executions.
</verification>

<success_criteria>
- TOOL-03: web_fetch returns readable text (not HTML) from a real URL
- TOOL-04: web_search returns structured results from Brave API (or clear "not configured" error if key absent)
- SAFE-04: tool_audit_log table populated with correct fields after tool use
- Regression: existing file/shell tools still work; approval gate intact
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-tools-and-audit/04-04-SUMMARY.md`
</output>
